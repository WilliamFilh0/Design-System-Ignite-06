{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "json-schema": "^0.4.0",
    "json5": "^2.2.3",
    "semver": "^7.6.3"
  },
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^2.4.0"
  },
  "packageManager": "npm@9.0.0"
}
